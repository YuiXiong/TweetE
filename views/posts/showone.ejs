<%- include('../partials/header-logged-in'); %>

<div class="w-full lg:w-6/12 px-4 mx-auto pt-6">
  <div class="relative flex flex-col min-w-0 break-words w-full mb-6 shadow-lg rounded-lg bg-blueGray-200 border-0">
    <div class="rounded-t mb-0 px-6 py-6">
      <div class="grid grid-cols-1 mx-auto gap-1">

          <form class="w-full max-w-lg pl-8" id="tweetE" method="POST" action="/post/<%= post.id %>?_method=PATCH">
            <input class="appearance-none block w-full bg-gray-200 text-gray-700 border border-red-500 rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white" 
            id="tweetE" name="updateTweetEContent" type="text" value="<%= post.tweetEContent %>">
            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">
            Edit
            </button>
          </form>
          
          <form class="w-full max-w-lg pl-8" id="tweetE" method="POST" action="/post/<%= post.id %>?_method=DELETE">   
            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" onclick="return confirm('Delete permanently?')" type="submit">
              DELETE</button>
          </form>

        <h1> comment section below</h1>

        <div>
          <form class="w-full max-w-lg" id="tweetE" method="POST" action="/post/<%= post.id %>/comment">
              <div class="flex flex-wrap mt-8 mb-3">
                <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-first-name">
                  comment this tweet!
                </label>
                <input class="appearance-none block w-full bg-gray-200 text-gray-700 border border-red-500 rounded py-3 px-4 mb-1 leading-tight focus:outline-none focus:bg-white" 
                id="commentData" name="commentData" type="commentData" placeholder="flame em!">
              </div>
            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit"><a href="#"></a>
                TweetE
            </button>
          </form>
        </div>  

        </div>
      </div>
    </div>
  </div>

<%- include('../partials/footer'); %>
